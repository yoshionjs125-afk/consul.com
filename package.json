{
  "name": "neutral-consultation-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Neutral consultation & diagnosis matching platform for housing decisions (Phase 1 core).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
